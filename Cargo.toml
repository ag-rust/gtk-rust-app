[package]
name = "gtk-app-framework"
version = "0.0.1"
edition = "2018"

[dependencies]
log = "0.4"

serde = { version = "1", features = ["derive"] }
toml = "0.5"

gettext-rs = "0.7"

# GTK dependencies
gtk = { version = "0.3", package = "gtk4" }
libadwaita = "0.1.0-alpha-6"
[dependencies.gdk4]
version = "0.3"
[dependencies.glib]
version = "*"
